raw_h5ad: data/raw/arc/adata_Training.h5ad
processed_npz: data/processed/perturbation_delta_dataset.npz
split_json: data/splits/gene_split_seed42.json

control_label: non-targeting
target_col: target_gene

batch_col: batch 
cell_type_col: cell_type

test_metadata: data/raw/test/test.csv
prediction_output: outputs/predictions/test_predictions.npz

seed: 42
val_fraction: 0.2

# Preprocessing
min_genes: 200
max_genes: 8000
max_pct_mt: 20
n_hvg: 5000 # change to None for no HVG selection
normalize_total: 1e4
use_delta: True